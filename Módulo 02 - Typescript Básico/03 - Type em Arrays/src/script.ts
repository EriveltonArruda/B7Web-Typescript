let nomes: string[] = ["Erivelton", "Pedro", "Everton"]; // Só aceitará strings no array
let idades: number[] = [90, 15, 35, 65]; // Só aceitará números no array
let numeros: Array<number> = [15, 20, 45, 60]; // Também pode ser feito assim

numeros.push("Erivelton"); // Não aceitará
